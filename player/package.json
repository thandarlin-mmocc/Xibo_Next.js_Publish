{
  "name": "xibo-player",
  "version": "0.1.0",
  "private": true,
  "description": "Kiosk player app - registers itself against the CMS, then displays content once approved.",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
